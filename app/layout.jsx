import './globals.css';

export const metadata = {
  title: 'DapLink | Official Wishlist',
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
